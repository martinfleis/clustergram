version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: miniforge3-latest

conda:
  environment: environment-dev.yml

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: doc/conf.py